import { type ReactElement } from 'react';

interface LanguageSwitcherProps {
  variant?: 'default' | 'icon';
}

/** Uygulama yalnizca Turkce destekler; dil secici gosterilmez. */
export function LanguageSwitcher(_props: LanguageSwitcherProps): ReactElement | null {
  return null;
}
