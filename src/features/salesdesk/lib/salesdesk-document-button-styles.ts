/** SalesDesk belge butonlari — index.css'teki .sd-doc-* zengin class'larina bagli (Navy Gold). */

/** Alt aksiyon bar butonlari icin taban olcu. */
export const SD_DOCUMENT_BUTTON_BASE =
  'h-10 w-full sm:w-auto rounded-xl px-6 font-bold border-0 transition-transform duration-200';

/** Kaydet — altin gradient + guclu isima. */
export const SD_DOCUMENT_BUTTON_SAVE = 'sd-doc-btn-primary';

/** Onizle — lacivert->mavi->teal gradient. */
export const SD_DOCUMENT_BUTTON_PREVIEW = 'sd-doc-btn-preview';

/** Kalem Ekle — birincil altin gradient CTA. */
export const SD_DOCUMENT_LINE_ADD_BUTTON =
  'sd-doc-btn-primary h-10 rounded-xl px-6 font-bold';

/** Kalem tablosu toolbar ikon kutusu — lacivert->altin gradient. */
export const SD_DOCUMENT_LINE_TOOLBAR_ICON =
  'sd-doc-toolbar-icon flex h-11 w-11 items-center justify-center rounded-xl';

/** Bolum numara rozeti (1,2) — altin gradient. */
export const SD_DOCUMENT_SECTION_BADGE =
  'sd-doc-badge flex h-8 w-8 shrink-0 items-center justify-center rounded-lg text-xs font-bold';

/** Ozet rozeti (3) — zumrut gradient. */
export const SD_DOCUMENT_SECTION_BADGE_SUMMARY =
  'sd-doc-badge-summary flex h-8 w-8 shrink-0 items-center justify-center rounded-lg text-xs font-bold';

/** Genel toplam gradient metni. */
export const SD_DOCUMENT_TOTAL_GRADIENT =
  'sd-doc-total-gradient text-lg font-black font-mono tabular-nums';
