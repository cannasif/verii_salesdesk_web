# (Opsiyonel) Backend API adresi. Yerel API icin ornek: http://localhost:5000
# Bos birakilirsa `npm run dev` istekleri Vite proxy uzerinden runtime-settings.json'daki API'ye yonlendirir.
# VITE_API_URL=http://localhost:5000

# (Opsiyonel) Yerel yardimci sunucu (Gmail + sohbet + gruplar).
# Gelistirmede bos birakilmasi onerilir; `npm run dev` sunucuyu otomatik baslatir ve Vite proxy kullanilir.
# Sadece frontend calistirmak icin: npm run dev:web (sunucu ayri: npm run server)
# VITE_GMAIL_BRIDGE_URL=
# VITE_CHAT_SERVER_URL=
